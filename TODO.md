# TODO - Fix AboutDirectorates React Issues

- [x] Fix invalid JSX structure in `AboutDirectorates.js` (missing wrapper tags / incorrect nesting of `<p>` elements).
- [ ] Run `npm test` / `npm run build` to confirm compilation succeeds.

